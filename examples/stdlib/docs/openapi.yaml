openapi: 3.0.3
info:
  title: Users API
  version: 1.0.0
paths:
  /api/users:
    get:
      operationId: listUsersAuth
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  properties:
                    email:
                      type: string
                    id:
                      type: string
                  type: object
                type: array
          description: OK
      security:
      - bearerAuth: []
      summary: List users (authenticated)
      tags:
      - users
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApiUser'
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiUser'
          description: Created
      summary: Create a user
      tags:
      - users
  /api/users/{id}:
    get:
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiUser'
          description: OK
      summary: Get user by ID
      tags:
      - users
components:
  schemas:
    ApiUser:
      properties:
        email:
          type: string
        id:
          type: string
        name:
          type: string
      type: object
  securitySchemes:
    apiKey:
      in: header
      name: X-API-Key
      type: apiKey
    bearerAuth:
      bearerFormat: JWT
      scheme: bearer
      type: http

openapi: 3.0.3
info:
  title: Content-Type API
  version: 1.0.0
paths:
  /report:
    get:
      operationId: getReport
      responses:
        "200":
          content:
            application/json:
              schema:
                type: string
            text/csv:
              schema:
                type: string
          description: OK
      summary: Get report
      tags:
      - report
  /upload:
    post:
      operationId: uploadFile
      requestBody:
        content:
          multipart/form-data:
            schema:
              format: binary
              type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentTypeUploadResponse'
          description: OK
        "400":
          content:
            application/json:
              schema:
                properties:
                  error:
                    type: string
                type: object
          description: Bad Request
      summary: Upload a file
      tags:
      - upload
components:
  schemas:
    ContentTypeUploadResponse:
      properties:
        filename:
          type: string
        id:
          type: string
        size:
          format: int64
          type: integer
      type: object
